apiVersion: v1
kind: Service
metadata:
  name: sidecar-injector
  namespace: istio-system
  labels:
    app: sidecar-injector
spec:
  ports:
    - port: 443
      targetPort: 443
    - port: 8080
      targetPort: 8080
  selector:
    app: sidecar-injector